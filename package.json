{
  "name": "hergla-park-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "build:dashboard": "cd dashboard && npm install && npm run build",
    "build:vr-landing": "cd vr-landing && npm install && npm run build",
    "dev:dashboard": "cd dashboard && npm run dev",
    "dev:vr-landing": "cd vr-landing && npm run dev",
    "install:all": "npm install && cd dashboard && npm install && cd ../vr-landing && npm install"
  }
}
